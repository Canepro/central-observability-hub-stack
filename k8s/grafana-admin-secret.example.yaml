apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
type: Opaque
stringData:
  # Replace before applying. Keep out of git if using real secrets.
  admin_password: "REPLACE_ME"
  secret_key: "REPLACE_ME"
