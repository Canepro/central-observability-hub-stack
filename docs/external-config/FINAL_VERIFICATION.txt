## Final Verification Steps

1.  **Check Logs Again**:
    ```bash
    podman logs -f grafana-agent
    ```
    You should now see messages like "agent running" and component initialization (prometheus.scrape, loki.source.docker).

2.  **Check Central Grafana**:
    -   Go to `https://grafana.canepro.me`
    -   Navigate to **Dashboards > Rocket.Chat Metrics**
    -   Verify that data is appearing (might take ~60 seconds).

3.  **Check Loki Logs**:
    -   Go to **Explore > Loki**
    -   Select Label: `container_name` = `rocketchat-deployment_rocketchat_1`
    -   Click **Run query**. You should see the application logs.

