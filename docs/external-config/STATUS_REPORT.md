## 1. Podman Verification (`podman.canepro.me`)
- [x] **Logs**: Agent started successfully (logs confirmed "starting complete graph evaluation" and "Done replaying WAL").
- [x] **Connection**: Agent is successfully pushing to `https://observability.canepro.me`.
- [ ] **Visual**: User to check Grafana dashboards for incoming data.

## 2. Kubernetes Setup (`k8.canepro.me`)
- [ ] **Deploy**: User to run the Helm command provided in `ROCKETCHAT-SETUP.md`.
- [ ] **Verify**: Check pods and logs on the K8s cluster.

## 3. RocketChat Config
- [ ] **Enable**: User to enable Prometheus in RocketChat Admin on both instances.

