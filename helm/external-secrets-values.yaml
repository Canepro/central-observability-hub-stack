# External Secrets Operator Helm values
# Chart: external-secrets/external-secrets

# CRDs must be installed out-of-band via server-side apply to avoid annotation size limits.
installCRDs: false

serviceAccount:
  create: true
  name: external-secrets

replicaCount: 1
